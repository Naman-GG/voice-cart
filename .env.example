# Speech recognition — Whisper large-v3 hosted on Groq.
# Create a free key at https://console.groq.com/keys, then copy this file
# to .env.local and paste the key below. Restart `npm run dev` afterwards.
GROQ_API_KEY = 
